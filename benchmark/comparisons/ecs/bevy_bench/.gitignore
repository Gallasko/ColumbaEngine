# Cargo build artifacts — regenerated by `cargo build`.
target/

# Resolved dependency lockfile. Pinned versions live in Cargo.toml; the lock
# isn't reproducibility-critical for a benchmark and changes constantly as
# transitive deps update.
Cargo.lock
