// GENERATED by tools/gen_vm_ops.pg from tools/vm_ops_def.pg — DO NOT EDIT.
// Regenerate with:  cmake --build <build-dir> --target GenerateVmOps
// Base stack/stack handlers: shared functors give them the same inline kernels as the fused paths.

    const DecodedInstruction* op_add_decoded(VM* vm, const DecodedInstruction& instr)
    {
        auto b = vm->pop();
        auto a = vm->peek();
        vm->changeTop(fusion::FAdd::apply(vm, a, b));
        // Escape analysis: only release heap objects, not primitives
        if (requiresRefCount(b)) vm->releaseAndDelete(b);
        return &instr + 1;
    }

    const DecodedInstruction* op_subtract_decoded(VM* vm, const DecodedInstruction& instr)
    {
        auto b = vm->pop();
        auto a = vm->peek();
        vm->changeTop(fusion::FSubtract::apply(vm, a, b));
        // Escape analysis: only release heap objects, not primitives
        if (requiresRefCount(b)) vm->releaseAndDelete(b);
        return &instr + 1;
    }

    const DecodedInstruction* op_multiply_decoded(VM* vm, const DecodedInstruction& instr)
    {
        auto b = vm->pop();
        auto a = vm->peek();
        vm->changeTop(fusion::FMultiply::apply(vm, a, b));
        // Escape analysis: only release heap objects, not primitives
        if (requiresRefCount(b)) vm->releaseAndDelete(b);
        return &instr + 1;
    }

    const DecodedInstruction* op_divide_decoded(VM* vm, const DecodedInstruction& instr)
    {
        auto b = vm->pop();
        auto a = vm->peek();
        vm->changeTop(fusion::FDivide::apply(vm, a, b));
        // Escape analysis: only release heap objects, not primitives
        if (requiresRefCount(b)) vm->releaseAndDelete(b);
        return &instr + 1;
    }

    const DecodedInstruction* op_modulo_decoded(VM* vm, const DecodedInstruction& instr)
    {
        auto b = vm->pop();
        auto a = vm->peek();
        vm->changeTop(fusion::FModulo::apply(vm, a, b));
        // Escape analysis: only release heap objects, not primitives
        if (requiresRefCount(b)) vm->releaseAndDelete(b);
        return &instr + 1;
    }

    const DecodedInstruction* op_equal_decoded(VM* vm, const DecodedInstruction& instr)
    {
        auto b = vm->pop();
        auto a = vm->peek();
        vm->changeTop(fusion::FEqual::apply(vm, a, b));
        // Escape analysis: only release heap objects, not primitives
        if (requiresRefCount(b)) vm->releaseAndDelete(b);
        return &instr + 1;
    }

    const DecodedInstruction* op_not_equal_decoded(VM* vm, const DecodedInstruction& instr)
    {
        auto b = vm->pop();
        auto a = vm->peek();
        vm->changeTop(fusion::FNotEqual::apply(vm, a, b));
        // Escape analysis: only release heap objects, not primitives
        if (requiresRefCount(b)) vm->releaseAndDelete(b);
        return &instr + 1;
    }

    const DecodedInstruction* op_greater_decoded(VM* vm, const DecodedInstruction& instr)
    {
        auto b = vm->pop();
        auto a = vm->peek();
        vm->changeTop(fusion::FGreater::apply(vm, a, b));
        // Escape analysis: only release heap objects, not primitives
        if (requiresRefCount(b)) vm->releaseAndDelete(b);
        return &instr + 1;
    }

    const DecodedInstruction* op_greater_equal_decoded(VM* vm, const DecodedInstruction& instr)
    {
        auto b = vm->pop();
        auto a = vm->peek();
        vm->changeTop(fusion::FGreaterEqual::apply(vm, a, b));
        // Escape analysis: only release heap objects, not primitives
        if (requiresRefCount(b)) vm->releaseAndDelete(b);
        return &instr + 1;
    }

    const DecodedInstruction* op_less_decoded(VM* vm, const DecodedInstruction& instr)
    {
        auto b = vm->pop();
        auto a = vm->peek();
        vm->changeTop(fusion::FLess::apply(vm, a, b));
        // Escape analysis: only release heap objects, not primitives
        if (requiresRefCount(b)) vm->releaseAndDelete(b);
        return &instr + 1;
    }

    const DecodedInstruction* op_less_equal_decoded(VM* vm, const DecodedInstruction& instr)
    {
        auto b = vm->pop();
        auto a = vm->peek();
        vm->changeTop(fusion::FLessEqual::apply(vm, a, b));
        // Escape analysis: only release heap objects, not primitives
        if (requiresRefCount(b)) vm->releaseAndDelete(b);
        return &instr + 1;
    }

